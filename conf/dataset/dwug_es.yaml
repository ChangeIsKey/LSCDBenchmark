name: dwug_es
language: es

task: lscd

grouping_1: 1
grouping_2: 2

uses:
  type: annotated # one of [all, sample, annotated]
  n: 100 # only in use if type == generated
  replacement: false # only in use if type == generated

pairing: EARLIER

preprocessing:
  method: keep_intact
  params: {}