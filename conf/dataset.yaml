name: dwug_es
language: es
task: lscd
metric: apd
grouping_1: 1
grouping_2: 2
uses:
  type: annotated # one of [sample, all, annotated]
  n: 100 # only in use if type == generated
  replacement: false # only in use if type == generated
pairing: COMPARE
preprocessing:
  type: lemmatize
  cached: false
